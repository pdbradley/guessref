# this script creates a branch (herok_deploy) that contains only the api folder
# and pushes it to heroku production

# taken from https://jtway.co/deploying-subdirectory-projects-to-heroku-f31ed65f3f2

git subtree split --prefix api -b heroku_deploy
git push production heroku_deploy:master --force
